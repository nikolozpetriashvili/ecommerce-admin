# .ENV file variables

```bash
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="check out clerk or create a new clerk project"
CLERK_SECRET_KEY="check out clerk or create a new clerk project"
DATABASE_URL="postgresql database"

NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME="check out cloudinary"

STRIPE_API_KEY="check out stripe or skip this because you do not really need cause it is not real project"
FRONTEND_STORE_URL=http://localhost:3001
STRIPE_WEBHOOK_SECRET="check out stripe or skip this because you do not really need cause it is not real project"
```
